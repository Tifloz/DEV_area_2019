import {StyleSheet} from 'react-native'

export default StyleSheet.create({
  
  button: {
        color: 'black',
  },

  bodyCard: {
    flex: 2,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },

  noAreaText: {
    textAlign: 'center',
    color: 'grey',
    fontSize: 16,
    paddingTop: 250
  }
});